library(skimr)
library(WVPlots)
library(plotly)
library(class)
library(MLmetrics)

#Goal: We want to see whether income and/or journey state can be used to predict question response

data <- read.csv("") # Import data
data

summary(data)
